#include <iostream>

int main(){
  //   int &a; // gives error, we must initialze referance
  // std::cout << a;
  //  int &a = 3; // gives error, 3 is rvalue, instead of it we must have lvalue
  //std::cout << a;
  //int &a = 3 + 5 - 9; // the same error as previous
  //std::cout << a;
  return 0;
}
